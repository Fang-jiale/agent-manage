{
  "name": "ywmatrix",
  "version": "1.0.0",
  "description": "YwMatrix — 远程 Agent 管理平台（Node.js 实现）",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "gateway": "node src/gateway.ts",
    "client": "node src/client.ts",
    "local-agent": "node src/local-agent.ts",
    "login": "node src/login.ts",
    "typecheck": "tsc --noEmit",
    "bundle": "esbuild src/gateway.ts src/client.ts src/local-agent.ts src/login.ts --bundle --platform=node --format=esm --target=node18 --outdir=dist/bundle --out-extension:.js=.mjs \"--banner:js=import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "test": "node --test --test-concurrency=1 'test/*.test.ts'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1097.0",
    "mysql2": "^3.23.2",
    "redis": "^6.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.8.0"
  }
}
